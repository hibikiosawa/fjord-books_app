<h1><%= t('views.common.userindex') %></h1>
 
<% @users.each do |user| %>
<li>
    <%= user.email %>
    <%= link_to t('views.common.show'),"/users/#{user.id}" %>
</li>
<% end %>

<%= paginate @users %>

<br>

<%= link_to t('views.common.back'),books_path %>