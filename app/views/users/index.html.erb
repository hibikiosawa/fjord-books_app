<h1><%= Book.model_name.human %></h1>
 
<% @users.each do |user| %>
<li>
    <%= user.username %>
    <%= link_to t('views.common.show'),"/users/#{user.id}" %>
</li>
<% end %>

<%= paginate @users %>

<br>

<%= link_to t('back'),books_path %>