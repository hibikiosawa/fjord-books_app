<h1><%= t('views.user.index') %></h1>
 
<% @users.each do |user| %>
<li>
    <%= user.email %>
    <%= link_to t('views.common.show'),user_path(user) %>
</li>
<% end %>

<%= paginate @users %>

<br>

<%= link_to t('views.common.back'), books_path %>